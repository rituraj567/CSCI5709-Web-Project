image: node
stages:
  - build
  - test
  - deploy

build:
  stage: build
  tags:
    - ugrad

  script:
    - npm install
    - npm run build
  artifacts:
    paths:
      - build/

test:
  stage: test
  tags:
    - ugrad

  script:
    - npm test
